# App / Expo — safe for client / Vercel (EXPO_PUBLIC_* only)
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# NEVER put the service role in .env or Vercel.
# Put it only in .env.publish (gitignored), then:
#   npm run publish:pathway
